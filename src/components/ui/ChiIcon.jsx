import React from 'react';

const ChiIcon = ({ className = 'w-4 h-4' }) => {
  return <img src="/assets/logo/logo_chi.png" alt="CHI" className={`inline-block align-middle object-contain ${className}`} />;
};

export default ChiIcon;

